import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header: {
        fontSize: 50,
        fontWeight: 800,
        color: '#000',
    },

    text: {
        fontSize: 18,
        fontWeight: 300,
        color: '#000'
    },
})